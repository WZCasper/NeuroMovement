# Зависимости времени выполнения NeuroMovement.
# Версии зафиксированы по набору, реально протестированному в CI/разработке.
#
# ПРИМЕЧАНИЕ: при первой установке pip автоматически скачает и CUDA-компоненты
# PyTorch (даже если у вас нет NVIDIA GPU) — это нормально, так стандартно
# устроен пакет torch на PyPI. Первая установка может занять несколько ГБ
# трафика и места на диске. Это разовая стоимость НАСТРОЙКИ окружения
# разработки/сборки — само собранное приложение (.exe) после этого работает
# полностью офлайн, без единого сетевого запроса.

numpy==2.5.1
opencv-python-headless==5.0.0.93
PySide6==6.11.1
mss==10.2.0
torch==2.12.1
torchvision==0.27.1
ultralytics==8.4.89

# Захват конкретного окна (а не всего монитора) поддерживается только в
# Windows и требует win32gui из пакета pywin32.
pywin32>=306 ; sys_platform == "win32"
